{
	"title": "Instagram Public Activity History Input",
	"type": "object",
	"schemaVersion": 1,
	"properties": {
		"username": {
			"title": "Target username",
			"type": "string",
			"description": "Public Instagram username to inspect. No Instagram login is required in the default public flow. The Actor accepts plain usernames with or without a leading @ and returns best-effort public activity results.",
			"pattern": "^@?[A-Za-z0-9._]+$",
			"editor": "textfield",
			"prefill": "nasa"
		}
	},
	"required": ["username"]
}
